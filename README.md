# Clinic SMM Manager v9

Версия v9 добавляет нормальную публикацию с заголовками:

- у каждого поста теперь есть короткий `headline`;
- если пост публикуется без изображения, заголовок ставится в начало текста жирным шрифтом;
- если пост публикуется с изображением, заголовок графически встраивается в картинку;
- изображение + текст публикуются одним сообщением через Telegram caption;
- если текст слишком длинный для одного сообщения с фото, бот попросит сократить пост командой `/rewrite`.

## Важное ограничение Telegram

У подписи к фото в Telegram есть лимит около 1024 символов. Поэтому формат “изображение + полный текст одним сообщением” возможен только для постов примерно до 900–1000 символов.

Если бот выдаст ошибку лимита, сократите текст:

```text
/rewrite 4 Сократи текст до 900 символов, сохрани смысл, пользу и мягкий CTA.
```

## Команды Telegram

```text
/start
/help
/generate тема
/generate_full тема
/posts
/post ID
/edit ID новый полный текст
/rewrite ID инструкция
```

Под карточкой поста есть кнопки:

```text
✅ Одобрить
🚀 Опубликовать
🖼 Картинка
❌ Отклонить
👁 Показать пост
✏️ Как редактировать
```

## Railway Variables

```env
APP_NAME=clinic-smm-manager
ENVIRONMENT=production
DATABASE_URL=...
OPENAI_API_KEY=...
OPENAI_MODEL=gpt-4.1-mini
OPENAI_IMAGE_MODEL=gpt-image-1
TELEGRAM_BOT_TOKEN=...
ADMIN_TELEGRAM_ID=...
PUBLIC_BASE_URL=https://твой-домен.up.railway.app
TELEGRAM_PUBLISH_CHAT_ID=-100...
GENERATED_IMAGES_DIR=storage/generated_images
```

## После обновления

После замены файлов на GitHub Railway выполнит деплой. Потом снова выполните в Swagger:

```text
POST /telegram/set-webhook
```


## v10: логика публикации картинки и длинного текста

Если у поста есть изображение:

1. Заголовок встраивается в изображение.
2. Если текст помещается в лимит Telegram caption — бот публикует фото и текст одним сообщением.
3. Если текст длиннее лимита caption — бот публикует два сообщения:
   - сначала изображение с заголовком внутри;
   - затем полный пост с жирным заголовком.

Если изображения нет, бот публикует одно сообщение: жирный заголовок + текст поста.


## v11

Исправлена публикация длинных постов с изображением: картинка отправляется без caption и без встроенного заголовка, полный текст идет отдельным сообщением. Для коротких постов улучшена отрисовка кириллического заголовка на изображении через DejaVu Sans/Matplotlib fallback.


## v12: удобное редактирование кнопками

Изменения:

- Убрана кнопка «Показать пост».
- Убрана кнопка «Как редактировать».
- Добавлена кнопка «✏️ Редактировать вручную». После нажатия бот просит прислать полный новый текст одним сообщением.
- Добавлена кнопка «🤖 ИИ-редактирование». После нажатия бот просит написать, что нужно исправить, и отправляет правку в ИИ.
- При ручном и ИИ-редактировании ранее созданная картинка сохраняется.
- Для отмены режима редактирования можно отправить `/cancel`.
